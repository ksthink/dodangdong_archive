-- 태그(item.tags)를 접는다.
--
-- 주제(dc:subject)를 두 갈래로 받았다: 정해진 목록인 주제분류(subject)와, 목록에 없는
-- 말을 받는 자유어 태그(item.tags). 그런데 태그는 어디서도 길이 되지 못했다 — 찾기가
-- 태그를 뒤지지 않았고, 상세에 글자로 적히기만 했지 누를 수 없었다. 혼례·이사·설날·추석·
-- 입학·졸업은 주제분류에도 태그에도 있어, 같은 말을 두 군데 적고 있기도 했다.
-- 그래서 관리 폼과 자료 상세에서 태그를 뺀다. 주제분류만 쓴다.
--
-- 칸은 지운다고 하지 않고 남긴다. 이미 49건에 스물여덟 가지가 적혀 있고, 그중 감나무·
-- 폐백·도장·제문처럼 주제분류에 없는 말이 있다. 지우면 그 말들이 함께 사라진다.
-- 저장 코드도 이 칸을 건드리지 않으므로, 적힌 값은 그대로 남는다.
comment on column item.tags is '접힌 칸(2026-09-23). 더는 쓰지도 보여 주지도 않는다. 예전에 적어 둔 자유어를 잃지 않으려고 남겨 둔다 — 주제는 주제분류(item_subject)로 받는다.';
