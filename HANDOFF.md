# 넘겨주는 글

2026-09-23 기준. 이어받는 사람(또는 다음 세션)이 **지금 어디까지 왔고, 왜 그렇게 정했으며, 무엇이 남았는지**
한자리에서 보라고 적는다. 방법과 규칙은 다른 문서에 있으니 여기서 되풀이하지 않는다.

| 무엇을 보려면 | 어디로 |
| --- | --- |
| 사이트가 무엇을 하는가, 어떻게 돌리는가 | [README.md](README.md) |
| 색·글자·간격·화면별 규칙(스크린샷 포함) | [DESIGN.md](DESIGN.md) |
| 코드를 고칠 때의 함정, 큰 그림 | [CLAUDE.md](CLAUDE.md) |
| DB 권한 규칙과 마이그레이션 목록 | [supabase/README.md](supabase/README.md) |
| 보안 점검 결과와 조치 현황 | [SECURITY.md](SECURITY.md) |

- 사이트: https://dodangdong-archive.vercel.app · 저장소: https://github.com/ksthink/dodangdong_archive (**공개**)
- 지금 판: `f14c233` (인트로 머리글의 `RELEASE 날짜 (커밋)` 이 배포된 판이다)

---

## 1. 지금 상태

**살아 있고, 잠겨 있고, 쓸 수 있다.** 자료 53건(공개 42) · 인물 12명 · 이야기 3편 · 파일 24개 · 녹취록 1건 · 묶음 6개.

| 갈래 | 상태 |
| --- | --- |
| 손님 읽기 | **없다.** 앱도 DB도 잠갔다(2026-09-23) |
| 보안 높음·중간 | H1·M1·M2 닫음. **M3(가입 끄기)만 남았고 대시보드 일이다** |
| 영상 | 재생 규격(H.264·AAC·moov 앞) 파일만 튼다. 규격 밖 원본은 보존 + 재생용 따로 |
| 인물 얼굴 | 사진에서 잘라 쓰는 길을 열었다. **아직 한 명도 넣지 않았다**(face 파일 0개) |
| 배포 | Vercel, 함수는 도쿄(hnd1) — Supabase(ap-northeast-1) 곁 |

---

## 2. 결정 사항 — 왜 그렇게 했나

되돌리고 싶어질 때 **까닭**을 먼저 읽으라고 적는다. 날짜는 그렇게 정한 날이다.

### 2-1. 사이트를 통째로 잠갔다 (09-23)

인트로(`/intro`)가 사이트의 문이다. 로그인하지 않으면 어떤 화면도 열리지 않는다.

- **까닭**: 운영자의 가족사 기록이고, 허가받지 않은 방문자를 들이지 않기로 정했다.
- **함께 바뀐 것**: DB의 손님(anon) 읽기 정책·권한을 전부 걷어냈다(0014). 앱만 잠그고 DB를 두면
  publishable 키로 REST를 곧장 불러 실명·주소·Drive id가 읽혔다(SECURITY.md H1 — 실제로 새고 있었다).
- **딸린 뜻 바뀜**: `access_level`(공개/비공개)은 이제 "손님에게 보이는가"가 아니라
  **"첫 화면 히어로에 걸리는가"** 다. `src/lib/hero.ts` 가 직접 거른다.
- **남은 옛말**: 코드 주석과 관리 화면 문구에 "손님에게 보임" 같은 말이 아직 남아 있다(3-3).

### 2-2. 인트로는 낡은 단말기, 그러나 흑백 (09-23)

CodePen의 *Fallout Terminal Inspired CSS Theme* 에서 만듦새를 가져왔다. 색은 녹색 형광체 대신 종이·먹색.

- **글꼴**: 처음에 Neo둥근모를 넣었다가 **갈무리로 되돌렸다.** 사이트 전체가 글꼴 하나라는 규칙이 더 값지다.
- **움직임**: 사이트의 `* { animation: none !important }` 를 `.intro` 안에서만 `!important` 로 되돌린다.
  깜빡임(형광체 떨림·커서)은 **뺐다** — 거슬린다는 판단. 남은 움직임은 8초마다 훑는 줄과 첫 방문의 부팅 연출뿐.
- **로고는 216px 고정**. 그림 속 줄무늬 한 주기가 그 크기에서 화면 주사선과 같은 3px 이 된다. 크기를 바꾸면 결이 어긋난다.
- **판 표시**: `RELEASE 2026-09-23 (f14c233)`. 날짜는 빌드한 때, 괄호는 커밋. `next.config.ts` 가 빌드할 때 박는다.
  판번호(`package.json`)는 화면에 내지 않고 GitHub 릴리스에만 쓴다.

### 2-3. 영상은 변환하지 않는다 (09-22)

서버에서 트랜스코딩하지 않는다. 규격 밖 영상은 **원본 그대로 보존**하고, 규격에 맞춘 사본을
관리자가 따로 올린다(`role='stream'`). 까닭: 서버 변환은 시간·비용·실패가 크고, 아카이브는 원본이 먼저다.

### 2-4. 인물 얼굴은 사진에서 잘라 쓴다 (09-23, 가장 최근)

- 얼굴 사진을 **따로 올리지 않는다.** 그 사람이 나오거나 만든 자료의 사진에서 네모를 잡아 잘라 쓴다.
  까닭: `file` 은 반드시 `item` 에 매여 있고, 얼굴에도 출처가 남아야 하며, 자료를 지우면 얼굴도 떨어져야 한다.
- **자르기는 원본에서** 한다. 썸네일(480px)에서 자르면 단체 사진 속 얼굴이 뭉개진다.
- **끌어서 옮기지 않는다.** 누르면 그 자리가 네모의 가운데가 되고 크기는 슬라이더로 바꾼다.
  이 사이트에는 끌어 움직이는 것이 하나도 없고, 이 방식이면 키보드로도 그대로 된다.
- `person.face_crop` 은 다시 자를 때 쓰는 자리이자 **"이 사진의 이 자리에 이 사람이 있다"는 기록**이다.
  나중에 사진 태깅으로 넓힐 여지를 남겨 둔 셈이다(3-2).

### 2-5. 접은 것 — 되살리기 전에 읽을 것

| 무엇 | 언제 | 까닭 |
| --- | --- | --- |
| 다크 모드 | 09-22 | 밝은 화면 하나뿐. `color-scheme: only light` |
| 자유어 태그(`item.tags`) | 09-23 | 찾기도 못 하고 누를 수도 없어 적기만 하던 칸. 주제분류와 말이 겹쳤다. **칸과 값은 남겨 두었다** |
| 꼬리말 로고 그림 | 09-23 | 글자 `©metaphr` 로 바꿨다 |
| 관계망(네트워크) 보기 | 09-22 | 사용자가 취소 |
| 자료 찾기 페이지네이션 | 09-22 | 사용자가 취소. **잠재 결함은 그대로 남아 있다**(3-1) |
| `date_verified`(증빙 확인) 손질 | 09-23 | 검토만 하고 그대로 두기로 했다. 지금은 42건 중 8건만 참 |

---

## 3. 남은 할 일

급한 것부터. 각 줄 끝의 문서를 보면 자세한 근거가 있다.

### 3-1. 곧 해야 하는 것

| | 무엇 | 어디에 |
| --- | --- | --- |
| 1 | **Supabase 대시보드에서 신규 가입 끄기** (Auth → Sign In / Providers). 지금은 가입이 열려 있으면 누구나 세션을 얻어 문을 지난다. DB가 0행을 주므로 빈 화면이지만, 잠근 사이트가 열리는 셈이다 | SECURITY.md M3 |
| 2 | 같은 화면에서 **Leaked password protection 켜기** | SECURITY.md L8 |
| 3 | **얼굴을 실제로 넣어 보기.** 길만 열어 두고 한 명도 넣지 않았다. 사진이 여럿인 사람(할머니·어머니 7장, 아버지 6장, 할아버지 4장)부터 | — |
| 4 | 원본 프록시에 `nosniff`·`Content-Disposition`, 올리는 쪽에 mime 허용 목록 | SECURITY.md L1 |
| 5 | 오류 본문의 상류 메시지 감추기, `register` 의 Drive id 검증, matcher 경계(`intro(?:/|$)`), 206 `no-store` | SECURITY.md L2·L3·L4·L5 |

### 3-2. 생각해 볼 것 (사용자 결정 필요)

- **얼굴 자르기를 써 본 느낌** — 누르기+슬라이더로 충분한지, 끌어서 옮기는 게 나은지.
- **크기 슬라이더가 브라우저 기본 파란색**이다. 픽셀 모양으로 바꿀지(DESIGN.md 12장 "알려진 틈"의 체크박스와 같은 부류).
- **사진 태깅으로 넓힐지** — 지금은 인물당 얼굴 하나(`person.face_crop`)다. (사진, 사람, 네모)를 따로 표로 두면
  "이 사진에 누가 어디 있는지"를 전부 적을 수 있다. 집안 아카이브에서 가장 빨리 사라지는 지식이라 값어치가 크다.
- **자료 찾기 100건 상한** — `.limit(100)` 이고 "전체 N건"은 가져온 개수다. 같은 날짜 정렬도 불안정하다.
  자료가 100건을 넘기 전에 손봐야 한다(지금 53건).
- **`access_level` 을 계속 둘지** — 손님이 없어져 "히어로에 걸리는가"라는 뜻만 남았다. 이 뜻을 유지할지 정한다.
- **`vercel` 개발 의존성** — `npm audit` 29건이 전부 여기서 온다. 빼고 `npx vercel@latest` 로 쓸지(SECURITY.md L9).

### 3-3. 손이 갈 때

- 코드 주석·관리 화면 문구에 남은 **"손님"** 옛말 정리(2-1).
- DESIGN.md 12장 "알려진 틈" — 체크박스 파란색, 이야기 속 큰 빈 디더, 브라우저 기본 재생기, 안 쓰는 토큰.
- `requireAdmin()` 이 옛 주소 `/login?next=…` 로 보낸다. `/intro` 로 바꾸면 한 번 덜 튄다.
- `src/lib/google/drive.ts`·`env.ts` 에 `import 'server-only'` 한 줄.
- 스크립트들이 `.env.local` 을 손으로 파싱한다. `node --env-file` 로 통일.
- `scripts/seed/fake-manifest*.json` 을 `.gitignore` 에.

---

## 4. 이어받는 사람이 밟을 함정

CLAUDE.md의 "함정" 절이 본체다. 그중 **이번에 실제로 밟은 것** 둘만 다시 적는다.

1. **`npm run build` 의 타입 오류를 놓치기 쉽다.** `✓ Compiled successfully` 를 찍은 **뒤에** 타입 검사를 하고,
   그 오류는 소문자(`error TS2345`, `Failed to type check.`)다. 출력을 `grep "Error"` 로 걸러 보다가
   실패한 커밋을 배포한 적이 있다(`a7da615`). **종료 코드를 보라.**
2. **배포가 조용히 실패할 수 있다.** Vercel은 빌드가 깨지면 마지막 성공 판을 계속 서비스한다.
   "배포가 오래 걸린다"로 보인다. `gh api repos/ksthink/dodangdong_archive/commits/<sha>/status --jq .state` 로 확인하거나,
   인트로의 `RELEASE … (커밋)` 이 바뀌었는지 본다.

## 5. 사람이 직접 해야 하는 일

- **비공개 자료가 제대로 막히는지 확인**과 **영상 올리기 시험**은 운영자가 직접 한다.
- 관리자 비밀번호는 어떤 파일에도 적지 않는다. 스크립트에는 `ADMIN_PASSWORD` 환경변수로만 넘긴다.
- Supabase 대시보드 일(가입 끄기 등)은 MCP로 할 수 없다.
